package aut.arch.local_data.models.enums

import aut.arch.R

enum class BowType(val src: Int) {
    TRADITIONAL(R.drawable.inactive_traditional),
    RECURVE(R.drawable.inactive_recurve),
    COMPOUND(R.drawable.inactive_compound)
}